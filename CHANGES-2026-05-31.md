# CHANGES — 2026-05-31 (Production Recovery Orchestrator)

Apply these to your git repo manually. Two source files changed, plus ledger
reconciliation. No destructive SQL, no schema changes, no dependency changes.
Manual-payment posture preserved; no Stripe/"pay now"/card copy introduced.

---

## Code changes

### 1. `src/app/admin/orders/[id]/invoice/page.tsx` — BUG-014 fix (P1)
**Why:** The invoice/report renderer read only `order.paymentMethodBrand` (a
Stripe-only field) and printed *"not yet captured"* for verified manual
bank-transfer orders, which populate `paymentMethodManual` instead. This is the
"payment method shows —" symptom from the browser audit.

**Change:** Added a fallback branch to the payment-method ternary:
```
{order.paymentMethodBrand ? ( <card brand …> )
  : order.paymentMethodManual ? ( <strong>{manual.toUpperCase().replace(/_/g,' ')}</strong> )   // NEW
  : ( <em>not yet captured</em> )}
```
So a verified BANK_TRANSFER order now prints **"Method: BANK TRANSFER"** on its
invoice. The other render sites (`admin/orders/[id]/page.tsx`,
`components/admin/OrderQuickView.tsx`) already used the shared `paymentLabel()`
helper, which already prefers `manual` — they were correct and were left
untouched. No SQL backfill needed: the data was already persisted; this was a
pure display/read bug.

### 2. `src/app/admin/page.tsx` — BUG-012 fix (P1)
**Why:** `/admin` Overview hydration-mismatch (React #418/#423/#425). The page is
a pure Server Component, so its `timeAgo()` (pure integer arithmetic) is
timezone-independent and cannot drift. The one genuinely TZ-dependent value was
`lastRefreshed`, built with `toLocaleTimeString('en-US', {…})` and **no
`timeZone`** — meaning it silently renders the *server's* clock to every viewer
(a real correctness/confusion bug as well as the hydration risk).

**Change:** Pinned the formatter to `timeZone: 'UTC'` and appended a ` ' UTC'`
label, so the value is deterministic across SSR/client and honest about which
clock it shows. The pre-existing `suppressHydrationWarning` wrappers (around
`lastRefreshed` and the relative-time KPI `sub`) were kept as defence-in-depth.

---

## Ledger / docs changes (no runtime impact)

### `BUGS.md`
- **Reconciled stale statuses.** The top "## OPEN" section still listed
  **BUG-001, BUG-002, BUG-004** as OPEN, but their fixes are present in the
  working copy (confirmed by code read this round). Corrected their headers to
  `FIXED (code; browser-unverified)` with a dated reconcile note each.
  - BUG-001: `publishProduct` gates SELLER to DRAFT→PENDING_REVIEW / PUBLISHED→DRAFT.
  - BUG-002: webhook pre-check + atomic `updateMany` status precondition.
  - BUG-004: `deleteProduct` blocks hard-delete on order history → ARCHIVE.
- **BUG-012 → FIXED (code; browser-unverified)** with full resolution note.
- **BUG-014 → FIXED (code; browser-unverified)** with full resolution note +
  audit of all render sites.

---

## Verification status (READ THIS)

- **File-tool (authoritative) confirms both edited files are complete and
  correct**, with the edits present: `page.tsx` ends cleanly at L781,
  `invoice/page.tsx` ends cleanly at L269 (`</main></>`).
- **`tsc --noEmit` could NOT be used as an authoritative gate this round.** The
  Linux shell mount served *stale, truncated* copies of exactly the two files
  edited (mount mtimes — May 28 / May 21 — predate today's edits, i.e. the mount
  never received the writes). tsc-via-shell therefore reported spurious
  "unterminated string / unclosed JSX" errors located precisely at each mount
  truncation point, and **reported zero errors in every other file** — confirming
  the rest of the project remains type-clean and these edits introduce no
  cross-file type errors.
- **ACTION FOR USER:** run `npm run typecheck` (and ideally `next build`) locally
  after applying, to close this verification gap. Edits are simple and were
  inspected for syntactic validity, but the shell could not produce a clean tsc
  pass due to the mount-sync issue.

## NOT done (still blocked / out of scope this round)
- No browser audit (Claude-in-Chrome for labtodate.com not exercised in this
  scheduled run) → nothing promoted to VERIFIED.
- BUG-013 (Stripe not configured) and BUG-005 (email verification) remain
  BLOCKED on user decision/config. BUG-011 (cart address form) needs UI work.
