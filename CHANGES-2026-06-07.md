# CHANGES — 2026-06-07 (orchestrator round: invariant code-verification sweep)

Apply these working-copy changes to git manually. `npx tsc --noEmit` exits 0
on the full set.

## Files changed

### 1. `src/lib/quotes/actions.ts` — BUG-023 (P0, financial)
- `sendProforma`: added an early **freeze guard** — if the linked order has
  left PENDING_PAYMENT and the re-issued price/currency differs, the action
  throws BEFORE any write (no partial state). Identical-price resend allowed.
- Existing-order totals rewrite changed `order.update` → atomic
  `order.updateMany({ where: { id, status: 'PENDING_PAYMENT' } })` so a
  concurrent payment freezes the amounts.
- On successful pre-payment rewrite, the quote line's
  `OrderItem.priceCentsSnapshot` is synced to the new price (keeps F1:
  totalCents == Σ items; the snapshot freezes at payment, not first issuance).
- The duplicate `prisma.order.findUnique` in the order-materialise block was
  removed (the guard's fetch is reused).

### 2. `src/app/api/auth/[...all]/route.ts` — BUG-025 (P1, security / invariant A14)
- POST handler now applies per-IP sliding-window rate limits (existing
  `lib/ratelimit.ts`) to credential-sensitive paths only:
  sign-in 10/15min · sign-up 5/h · forget/reset-password 5/15min ·
  magic-link 5/15min → HTTP 429 `{ message }`. GET and other POSTs untouched.

### 3. `src/app/marketplace/[slug]/page.tsx` — BUG-024 (P2, security/privacy / invariant S10)
- Detail page now 404s non-PUBLISHED products (DRAFT/PENDING_REVIEW/ARCHIVED)
  for everyone except the owning seller and ADMINs (preview retained).
- `generateMetadata` returns "Not found" for non-PUBLISHED (title leak closed).
- NOTE: this file's tail was truncated by an editor-tool fault mid-round and
  restored from `git show HEAD` (lines after the edit point are byte-identical
  to HEAD apart from CRLF preservation). Please eyeball the diff: expected
  changes are ONLY in `generateMetadata` and the new visibility gate block.

### 4. `BUGS.md`
- BUG-009/BUG-010 stale FIX_IN_PROGRESS headers reconciled → FIXED (code).
- New entries BUG-023/024/025; invariant reconciliation notes (F11, F15, S4).

### 5. `INVARIANTS.md`
- F11 🟡→✅ code · F15 ⏳→✅ · S4 ⏳→✅ (both paths) · A14 ⏳→✅ code (was
  effectively ❌) · S10 ⏳→✅ code.

## Not changed (deliberately)
- BUG-005 (email verification) — still BLOCKED on rollout decision.
- BUG-013 (Stripe on prod) — still awaiting user decision A/B.
- BUG-015/016/020 — infra/config items, owner-side, no repo change possible.
- BUG-019 (archive lacks buyer-facing closure) — needs a small product
  decision (terminal-status prompt vs buyer-visible "closed"); left open.
